[user]
    name = rexcyrio
    email = stefancyrio@gmail.com
[core]
    # disable all automatic CRLF / LF conversions
    autocrlf = false
    # warn when EOL conversions modify the file irreversibly
    safecrlf = true
    ignoreCase = false
    editor = vim
    eol = lf
[alias]
    graph = log --pretty=format:\"%C(auto)%h %ad | %s%d [%an]%C(reset)\" --graph --date=short
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
[credential]
    helper = manager
[credential "helperselector"]
    selected = manager
